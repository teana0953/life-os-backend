# 擱置中(2026-08-06)

設計完成、**未實作**。四輪 proposal review 的結論都在 `design.md` / `tasks.md` 裡。

**為什麼擱置**:四輪找到的產品缺口有三個屬於**貸款**那半 —— 寬限期表達不了、清償金額不能用「剩餘期數之和」(含未來利息)、回溯建立會回頭觸發已結束月份的預算警示。而過程中發現一個更該先做的:**分帳的自付額完全沒有算進預算**(backend#75)。

**重啟時**:
- backend#75 做完之後,「分期支援分帳」不用做任何事 —— 分帳自付額本來就會進個人記帳,分期是使用者跟銀行之間的事。
- 考慮**只做信用卡那半**(總額 ÷ 期數、一次清償),貸款另開。三個缺口有三個直接消失。
- **固定期限的訂閱(綁約 N 個月)結構上就是分期**,用「每期金額 + 期數」那條路徑。
- **無限期的月繳/年繳訂閱**需要 tick(`run-care-tick` 是既有的機制),獨立 feature。
