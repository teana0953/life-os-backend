{
  "findings": [
    {
      "severity": "blocking",
      "level": "code",
      "note": ".github/workflows/deploy.yml 與 ci.yml — 兩個 workflow 皆未宣告 top-level `permissions:`,GITHUB_TOKEN 沿用 repo 預設(可能為 read/write-all),違反最小權限。任一第三方 action 被入侵即可用該 token 改動 repo。deploy job 根本不需要 GITHUB_TOKEN(部署用 CLOUDFLARE_API_TOKEN),CI 也只需讀取。建議兩檔都加 `permissions:\\n  contents: read`(deploy 若無其他需求可用 `permissions: {}` 或 `contents: read`)。此為 task 明列的權限風險審查點。"
    },
    {
      "severity": "non_blocking",
      "level": "code",
      "note": ".github/workflows/deploy.yml — deploy job 沒有 concurrency 設定。連續兩次 push 到 main 會並行跑兩個 deploy,兩個 `drizzle-kit migrate` 可能競態、部署順序不定。建議加 `concurrency:\\n  group: deploy-main\\n  cancel-in-progress: false`(部署中途不可取消,故 false),序列化 main 部署。"
    },
    {
      "severity": "non_blocking",
      "level": "code",
      "note": "多處第三方 action 釘在可變 major tag:actions/checkout@v4、actions/setup-node@v4、cloudflare/wrangler-action@v3(deploy.yml/ci.yml)。可變 tag 會被上游重新指向,存在供應鏈風險。安全強化可釘完整 commit SHA(搭配 Dependabot 更新)。屬常見實務,標 non_blocking。"
    },
    {
      "severity": "non_blocking",
      "level": "code",
      "note": "scripts/lint-actions.sh:19-21 — 透過 `curl ... raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash | bash -s -- latest` 安裝:下載腳本取自可變的 `main` 分支並直接 pipe 到 bash,且安裝 `latest`(未釘版本)。供應鏈與可重現性風險。建議把安裝腳本 ref 釘到某個 release tag,並把 `latest` 改為固定 actionlint 版本。此為本機/gate 工具腳本,故 non_blocking。"
    },
    {
      "severity": "non_blocking",
      "level": "code",
      "note": ".github/workflows/ci.yml:3-7 — 同時觸發 `pull_request` 與 `push`(branches-ignore main)。同 repo 分支開 PR 時 push 與 pull_request 兩事件都會觸發,且兩者 github.ref 不同故 concurrency group 不同 → CI 會重複跑兩次,浪費 runner。常見做法是只留 pull_request(+ 可選 push 到 main,但此處 main 已由 deploy 處理)。"
    },
    {
      "severity": "non_blocking",
      "level": "code",
      "note": "spec 要求「Malformed workflow caught before merge → actionlint gate 失敗」,但 ci.yml 並未執行 `npm run lint:actions`;actionlint 只在本 change 的 dev-loop gate 跑一次。日後 PR 若改動 workflow YAML,CI 不會自動用 actionlint 攔截。若要讓 spec 情境對每個 PR 持續生效,建議在 ci.yml 增一步 `npm run lint:actions`(或視為僅本 change 的 gate,則屬設計取捨)。"
    },
    {
      "severity": "non_blocking",
      "level": "code",
      "note": ".github/workflows/deploy.yml Smoke test — 兩個 curl 皆無 `--retry`/`--max-time`。剛部署後 Worker 冷啟或 workers.dev 傳播可能有短暫延遲,煙霧測試偶發性 red 會誤判部署失敗。建議加 `--retry 3 --retry-connrefused --max-time 10` 降低 flakiness。狀態碼判斷(恰 200 / 恰 401、-f 對 health、-s 對 /api/me)本身嚴謹,無誤。"
    }
  ]
}
