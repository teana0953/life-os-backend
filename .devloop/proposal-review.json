{
  "findings": [
    {
      "severity": "non_blocking",
      "level": "design",
      "note": "煙霧測試如何取得已部署 URL 未在 design.md 或 tasks.md 3.4 指定,只寫「取得部署 URL」。可行路徑存在(wrangler-action 的 `deployment-url` output,或固定 `https://life-os-backend.<account-subdomain>.workers.dev`),不阻塞;但需在實作前定案。注意:`deployment-url` output 在部分 wrangler 版本回傳的是 version-preview URL 而非穩定 production workers.dev URL。建議實作時實測驗證,或改用固定 workers.dev 網址(account subdomain 由使用者填入 README/secret),避免 smoke test 打到錯誤或不穩定的 URL。"
    },
    {
      "severity": "non_blocking",
      "level": "design",
      "note": "wrangler-action 的 `secrets` 輸入只吃「變數名稱清單」,對應的值必須同時在該 step 的 `env:` 區塊提供(例如 `env: { DATABASE_URL: ${{ secrets.DATABASE_URL }}, FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }} }`)。design.md/tasks.md 3.3 只說「以 action 的 secrets 輸入上傳」,未點出這個 env 映射。若實作時漏掉,secret 會以空值上傳 → Worker runtime 無 DATABASE_URL → /health 回 503 → smoke test 會擋下(不致密鑰外洩,也不會靜默通過)。機制本身正確,故非阻塞,但 task 3.3 應明列 env 映射以免每次 CD 都紅。"
    },
    {
      "severity": "non_blocking",
      "level": "proposal",
      "note": "CI 觸發同時掛 `pull_request` 與非 main 分支 `push`,對同一 repo 的功能分支會造成重複 run(push 一次、開 PR 又一次)。單人開發影響輕微,可加 `concurrency` group 或只保留其一;非阻塞的最佳化。"
    }
  ]
}
